doctype html
html
  head
    title Sing in
  style
  body
    h1 Enter your data
    if isFall 
        h1 You enterd not correct login or password. Please, retype it
    form(method="POST" action="/singIn")
      p Login
      input(type="text" name="login") 
      br
      p Password 
      input(type="password" name="password")
      br
      p Select your mode
      select(name="mode")
        option(value="1" selected) Pupil 
        option(value="2") Teacher
      br
      br
      button(type="submit") Sing in